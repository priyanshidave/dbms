1.CREATE TABLE EMPLOYEE1(EMPNO NUMBER(10),NAME VARCHAR2(20),SALARY DECIMAL(10,2),DEPTNO NUMBER(10),DESIGNATION VARCHAR2(20));
-->CREATE OR REPLACE TRIGGER EMPLOYEE  
    BEFORE INSERT ON EMPLOYEE1 
BEGIN
  DBMS_OUTPUT.PUT_LINE ('New employees are about to be added'); 
END
-->INSERT INTO EMPLOYEE1(EMPNO,NAME,SALARY,DEPTNO,DESIGNATION)VALUES(1,'HARI',15000,1,'PROGRAMMER');

2. CREATE TRIGGER RAISE_LIMIT
     AFTER UPDATE ON EMPLOYEE1
     REFERENCING NEW AS N 
     FOR EACH ROW
     WHEN (N.SALARY > 2000)
BEGIN
    DBMS_OUTPUT.PUT_LINE('OLD SALARY : ' || :OLD.SALARY);
    DBMS_OUTPUT.PUT_LINE('NEW SALARY : ' || :N.SALARY); 
END

-->UPDATE EMPLOYEE1 SET SALARY=30000 WHERE EMPNO=1;